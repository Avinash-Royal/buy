<?xml version="1.0" encoding="utf-8"?>
<ScrollView xmlns:android="http://schemas.android.com/apk/res/android"
            xmlns:app="http://schemas.android.com/apk/res-auto"
            style="@style/Scrollview_Style"
            android:background="@drawable/back2">

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="vertical">

        <android.support.v7.widget.CardView
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_alignParentBottom="true"
            android:layout_margin="15dp"
            app:cardBackgroundColor="#44000000"
            app:cardCornerRadius="7dp"
            app:cardElevation="3dp"
            app:cardUseCompatPadding="false">

            <CalendarView
                android:id="@+id/simpleCalendarView"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:dateTextAppearance="@style/date"
                android:firstDayOfWeek="2"
                android:weekNumberColor="#fff"
                android:theme="@style/date"
                android:selectedWeekBackgroundColor="@android:color/holo_blue_dark"
                android:weekDayTextAppearance="@style/week"/>
        </android.support.v7.widget.CardView>

        <ListView
            android:id="@+id/list"
            android:layout_marginTop="10dp"
            android:layout_width="match_parent"
            android:layout_height="wrap_content">
        </ListView>
    </LinearLayout>

</ScrollView>
